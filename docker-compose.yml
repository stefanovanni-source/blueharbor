services:
  blueharbor:
    build: .
    image: blueharbor:latest
    container_name: blueharbor
    ports:
      - "8080:8080"
    environment:
      - ASPNETCORE_ENVIRONMENT=Development
    restart: unless-stopped
